<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Discovery is UDP multicast, which the Wi-Fi driver filters out unless a
         MulticastLock is held (RetsendActivity takes one). -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Received files have to land where an emulator or file manager can read
         them, and any file on the card has to be sendable, both through plain
         file I/O. That is what all-files access grants; without it the app falls
         back to its own external directory. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true">

        <!-- The launcher entry is the permission gate, not SDL: the paths it
             hands the Rust side depend on what has been granted, and they are
             read once at startup. -->
        <!-- Translucent, not NoDisplay: it has to survive the trip to the
             permission screen and finish only once the app has been started. -->
        <activity
            android:name="com.retsend.RetsendLauncherActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Landscape, either way up: the UI is the handhelds' 640x480 one, and
             a locked aspect also keeps it clear of egui-sdl2's cached window
             size, which a rotation can outrun (Android resizes the surface
             without always sending the event). -->
        <activity
            android:name="com.retsend.RetsendActivity"
            android:label="@string/app_name"
            android:screenOrientation="sensorLandscape"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="false" />
    </application>

</manifest>
